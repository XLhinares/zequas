name: zequas
description: A Flutter project to teach how to solve simple equations

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 3.0.0+9

environment:
  sdk: ">=2.17.0 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # framework
  get: ^4.6.5
  get_storage: ^2.1.1
  flutter_native_splash: ^2.4.7
  x_containers: ^1.2.4
  fraction: ^5.0.1
  flutter_emoji: ^2.4.0
  expandable: ^5.0.1
  flutter_launcher_icons: ^0.14.4
  flutter_typeahead: ^6.0.0
  flutter_markdown_plus: ^1.0.7
  simple_animations: ^5.2.0
  auto_size_text: ^3.0.0
  package_rename: ^1.10.1

dev_dependencies:
  lint: ^2.1.2
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# Flutter launcher icon configuration
# To re-set it, run `flutter pub run flutter_launcher_icons:main`
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/ic_launcher.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

# PACKAGE CONFIG ============================================================

# dart run package_rename
package_rename_config:
  android:
    app_name: "Zequas"
    package_name: "org.xeppelin.zequas"

  linux:
    app_name: "Zequas" # Window title
    package_name: "org.xeppelin.zequas" # Application ID
    exe_name: "zequas" # Executable name (binary)

  web:
    app_name: "Zequas" # App title & PWA name
    short_app_name: "Zequas" # PWA short name (defaults to app_name)
    description: "An app to teach how to solve simple equations." # Web app description

# dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icons/ac_launcher.png"
  image_path_android: "assets/icons/ac_launcher.png"
  adaptive_icon_foreground: "assets/icons/ac_launcher_foreground.png"
  adaptive_icon_background: "#282627"
  min_sdk_android: 21 # android min sdk min:16, default 21

# dart run flutter_native_splash:create
# dart run flutter_native_splash:remove
flutter_native_splash:
  color: "#282627"
  image: "assets/icons/ac_launcher.png"
  fullscreen: true
  #! ANDROID 12+ configuration:
  android_12:
    color: "#282627"
    # Please note that the splash screen will be clipped to a circle on the center of the screen.
    # with background: 960×960 px (fit within circle 640px in diameter)
    # without background: 1152×1152 px (fit within circle 768px in diameter)
    image: "assets/icons/ac_launcher.png"
    # icon_background_color: "#282627" # Optionnal
